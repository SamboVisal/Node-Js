<!DOCTYPE html>
<html lang="en">

{{> _head}}

<body>
  {{> _header}} {{#if user}} {{> _add-btn}} {{else}} {{/if}}
  <main>
    <div class="container-fluid">
      <div class="container">
        {{> _errors}} {{> _msg}}
      </div>

      {{{body}}}
    </div>
  </main>
  {{> _footbar}}
  {{> _script}}
</body>

</html>